# Build artifacts
build/
build-*/

# Local .uf2 builds, kept out of the repo. A RELEASED binary lives in UF2/
# and is committed; this directory is just the one you drag onto the Pico.
FLASHME/*.uf2

# Editor / OS noise
.vscode/
*.swp
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Rendered reference audio from the tools - regenerate as needed.
tools/*.wav
